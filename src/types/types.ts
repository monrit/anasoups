export type SoupType = {
  id: number;
  name: string;
  image: string;
  price: number;
  grams: number;
  description: string;
};

export type FormInputsType = {
  name: string;
  number: string;
};
